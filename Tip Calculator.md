```
def main():
    dollars = input("How much was the meal? ")
    percent = input("What percentage would you like to tip? ")

    # using function Ex. function1(dollars) * function2(percent)
    # TODO

    # round the tip to the nearest two decimal points
    # TODO

    print("Leave $",tip)

# replace $ to "", and change data type to float, then return d
def dollars_to_float(d):
    # TODO
    return d

# replace % to "", and change data type to float, (p / 100), then return p
def percent_to_float(p):
    # TODO
    return p

main()

```
